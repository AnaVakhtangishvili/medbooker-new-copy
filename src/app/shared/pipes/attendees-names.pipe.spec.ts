import { AttendeesNamesPipe } from './attendees-names.pipe';

describe('AttendeesNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new AttendeesNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
